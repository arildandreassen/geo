name: Deploy to AWS

concurrency: deploy

on:
  pull_request:
    branches:
      - main

jobs:
  deploy-dev:
    uses: ./.github/workflows/deploy-to-aws.yaml
    secrets: inherit
    with:
      targetEnv: 'dev'