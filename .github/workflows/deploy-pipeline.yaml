name: Deploy to AWS

concurrency: deploy

on:
  push:
    branches:
      - main

jobs:
  deploy-dev:
    uses: ./.github/workflows/deploy-to-aws.yaml
    with:
      environment: development
      targetEnv: 'dev'